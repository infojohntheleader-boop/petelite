{
  "name": "petelite-shopify-theme",
  "version": "1.0.0",
  "description": "Petelite — Premium DTC Pet E-Commerce Shopify Theme",
  "private": true,
  "scripts": {
    "dev": "shopify theme dev",
    "build": "shopify theme check",
    "lint": "shopify theme check",
    "push": "shopify theme push",
    "pull": "shopify theme pull",
    "watch": "shopify theme dev --notify ~/shopify-theme-notifications.log"
  },
  "dependencies": {
    "@shopify/oxygen": "^latest"
  },
  "devDependencies": {
    "@shopify/cli": "^3.0.0",
    "@shopify/cli-theme": "^3.0.0",
    "tailwindcss": "^4.0.0"
  }
}
