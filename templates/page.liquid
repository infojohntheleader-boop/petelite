<div class="page-container" style="max-width: {{ settings.container_width }}px; margin: 0 auto; padding: var(--section-spacing, 40px) 20px;">
  <h1>{{ page.title }}</h1>
  <div class="page-content">
    {{ page.content }}
  </div>
</div>

<style>
  .page-container {
    min-height: 400px;
  }
  
  .page-content {
    line-height: 1.8;
    color: #333;
  }
  
  .page-content img {
    max-width: 100%;
    height: auto;
    margin: 20px 0;
  }
</style>
